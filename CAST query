/* 

CAST 

It allows transforming any value from type X to Y. So we can change, for example, from date to datetime.

*/

---------------------------------------------

PRIMARY FORMULA 

  SELECT name of the basic column,
  CAST(name of the column to edit AS the value to which we want to change)
  AS name of the column in which we want the result
  FROM name of the table from which we perform the operation

---------------------------------------------
